name: attendance-app
root: ~/Projects/attendance/attendance-app
environment:
  JAVA_HOME: "$(/usr/libexec/java_home -v 1.8)"
  ANDROID_SDK_ROOT: "/Users/neiro/Library/Android/sdk/"
  ANDROID_HOME: "/Users/neiro/Library/Android/sdk/"
windows:
  - editor:
      layout: main-vertical
      focus: true
      panes:
        - v
  - dev:
      layout: main-vertical
      panes:
        - re-natal use-android-device avd && re-natal use-figwheel && le repl
  - misc:
      layout: main-vertical
      panes:
        - react-native run-android
        - caffeinate -u -t 547584758 &
  - api:
      layout: main-vertical
      root: ~/Projects/attendance/attendance-api
      panes:
        - v
        - le repl
